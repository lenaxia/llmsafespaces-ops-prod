---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # kube-system already exists (EKS-created); we don't own its lifecycle.
  # No namespace.yaml here — including one would fight EKS's own labels.
  - ./cilium/ks.yaml
